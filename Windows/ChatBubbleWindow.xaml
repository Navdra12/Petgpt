<Window x:Class="PetGPT.Windows.ChatBubbleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Width="500"
        Height="650"
        MinWidth="360"
        MinHeight="420"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="#F7F7F7"
        ShowInTaskbar="False"
        Topmost="True"
        KeyDown="OnKeyDown">
    <Border BorderBrush="#D0D0D0"
            BorderThickness="1"
            CornerRadius="14"
            Background="#F7F7F7">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#EEEEEE">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0"
                      VerticalAlignment="Center"
                      Margin="12,0,4,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="PetGPT"
                               FontWeight="SemiBold"/>
                    <TextBlock x:Name="PetChatsStatusText"
                               Grid.Column="1"
                               Margin="8,0,0,0"
                               Foreground="#666666"
                               FontSize="11"
                               TextTrimming="CharacterEllipsis"/>
                </Grid>

                <StackPanel Orientation="Horizontal"
                            Grid.Column="1"
                            HorizontalAlignment="Right">
                    <Button x:Name="NewPetChatButton"
                            Content="New" Width="44" BorderThickness="0"
                            ToolTip="New PetChat" Click="OnNewPetChat"/>
                    <Button x:Name="HistoryButton"
                            Content="History" Width="54" BorderThickness="0"
                            ToolTip="PetChats history" Click="OnHistory"/>
                    <Button Content="↗" Width="36" BorderThickness="0"
                            ToolTip="Open in browser" Click="OnOpenBrowser"/>
                    <Button Content="↻" Width="36" BorderThickness="0"
                            ToolTip="Reload" Click="OnReload"/>
                    <Button Content="×" Width="36" BorderThickness="0"
                            ToolTip="Close" Click="OnClose"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <wv2:WebView2 x:Name="ChatWebView"/>

                <Border x:Name="BrowserStatusPanel"
                        Visibility="Collapsed"
                        Background="#F7F7F7"
                        Padding="24">
                    <StackPanel VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                MaxWidth="360">
                        <TextBlock x:Name="BrowserStatusText"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="0,0,0,16"/>
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <Button Content="Hide"
                                    Padding="16,6"
                                    Margin="0,0,8,0"
                                    Click="OnClose"/>
                            <Button Content="Exit PetGPT"
                                    Padding="16,6"
                                    Click="OnExit"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
